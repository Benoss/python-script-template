{%- if project_type == "django" %}
// Tailwind CSS 4.1+ uses CSS-based configuration in input.css
// This file is no longer needed but kept for compatibility if needed
// Configuration is now done via @theme in static/css/input.css
{% endif %}
