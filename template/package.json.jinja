{% if project_type == "django" %}
// This project uses bunx to run Tailwind CSS without Node.js/npm dependencies
// No package.json needed - use 'task tailwind-watch' or 'task tailwind-build'
// Commands use: bunx @tailwindcss/cli@latest
{% endif %}
